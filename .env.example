# ===========================================
# LORAPP - Docker Environment Configuration
# ===========================================
# Copy this file to .env and fill with your actual values
# IMPORTANT: Never commit .env to git!

# ===========================================
# PostgreSQL Database
# ===========================================
POSTGRES_DB=lorapp
POSTGRES_USER=lorapp_user
POSTGRES_PASSWORD=change_this_secure_password_123

# Database URL (used by backend)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://lorapp_user:change_this_secure_password_123@postgres:5432/lorapp

# ===========================================
# JWT Authentication
# ===========================================
# Generate with: openssl rand -hex 32
SECRET_KEY=your-super-secret-jwt-key-generate-with-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ===========================================
# Google Cloud Vision API (OCR)
# ===========================================
# Path to credentials file (mounted as volume)
GOOGLE_APPLICATION_CREDENTIALS=/app/google-credentials.json

# ===========================================
# Google OAuth (Login with Google)
# ===========================================
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ===========================================
# Web Push Notifications (VAPID Keys)
# ===========================================
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key-starts-with-BP
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_CLAIM_EMAIL=mailto:admin@yourapp.com

# ===========================================
# File Upload Settings
# ===========================================
UPLOAD_DIR=/app/uploads
MAX_UPLOAD_SIZE=10485760

# ===========================================
# CORS Settings
# ===========================================
# Frontend URL for CORS (adjust for production)
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ===========================================
# Application Settings
# ===========================================
APP_NAME=Lorapp
APP_VERSION=1.0.0
DEBUG=False

# ===========================================
# Frontend Build Arguments
# ===========================================
# API URL for frontend (adjust for production)
VITE_API_URL=http://localhost:8000

# ===========================================
# Docker Port Mappings (optional customization)
# ===========================================
POSTGRES_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=3000

# ===========================================
# PRODUCTION CHECKLIST
# ===========================================
# Before deploying to production, ensure:
# ✓ POSTGRES_PASSWORD is strong and unique
# ✓ SECRET_KEY is generated with openssl rand -hex 32
# ✓ VAPID keys are generated with npx web-push generate-vapid-keys
# ✓ google-credentials.json is placed in backend/ folder
# ✓ GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET are from production OAuth app
# ✓ FRONTEND_URL and ALLOWED_ORIGINS point to production domain
# ✓ VITE_API_URL points to production backend
# ✓ DEBUG=False for production
# ✓ This .env file is NOT committed to git
